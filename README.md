# mpi_delta_stepping
MPI based delta stepping algorithm on SSSP(single source shortest path)



This repository is my implementation of Delta Stepping algorithm as the parallel and distributed system course project. Descriptions of the problem can be found in /des and source codes are under /src.

